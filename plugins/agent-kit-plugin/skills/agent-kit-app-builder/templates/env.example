# ============================================
# Hedera Account Configuration
# Required for ALL apps (Category A and B)
# Get a free testnet account at https://portal.hedera.com
# ============================================
HEDERA_OPERATOR_ID=0.0.XXXXX

# Private key — supports both formats (auto-detected):
#   DER-encoded: starts with "302e..." or "3030..."
#   Hex (ECDSA):  64-character hex string
HEDERA_OPERATOR_KEY=302e020100300506032b6570...

# Network: "testnet" (default, free) or "mainnet" (real HBAR costs)
HEDERA_NETWORK=testnet

# ============================================
# LLM Provider — Category B (agentic) apps ONLY
# Category A apps (e.g., Meme Coin Launchpad) do NOT need these.
# Category B apps (e.g., DeFi Agent) require exactly ONE provider.
# Pick one and uncomment:
# ============================================
# OPENAI_API_KEY=sk-...              # GPT-4o — reliable, broad tool-calling support
# ANTHROPIC_API_KEY=sk-ant-...       # Claude Sonnet — strong multi-step reasoning
# GROQ_API_KEY=gsk_...               # Llama 3.3 70B — fastest, free tier available
