{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if [ -f \".claude/scripts/post-edit-check.sh\" ]; then bash .claude/scripts/post-edit-check.sh; fi'"
          }
        ]
      }
    ]
  }
}
